//formerly ES_CW_DEBUGGROUPS//split into separate statements to support specific bizserver log debug (showDebug = 3) by individual as needed


if (currentUserID == "JCHALK")  showDebug = true;
if (currentUserID == "DHEDD")   showDebug = true;


